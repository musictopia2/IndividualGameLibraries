namespace SequenceDiceCP.Data
{
    //specify any other colors here.
    public enum EnumColorChoice
    {
        None, Blue, Red, Green
    }
    public enum EnumGameStatusList
    {
        None = 0, // so it can be saved
        RollDice = 1,
        MovePiece = 2
    }
}