namespace ThreeLetterFunCP.Data
{
    public class WordInfo
    {
        public string Word { get; set; } = ""; //because they don't allow a property with the same name as the class.
        public bool IsEasy { get; set; }
    }
}